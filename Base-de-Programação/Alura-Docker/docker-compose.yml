version: '3.8'

services:
  backend-api:
    build: ./backend-teste-docker
    ports:
      - "5000:5000"
    container_name: api-gabriel

  frontend-site:
    build: ./frontend-teste-docker
    ports:
      - "8080:80"
    container_name: site-gabriel
    depends_on:
      - backend-api